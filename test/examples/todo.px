{- Todo app -}
let todo_item = {
  title: nil,
  completed: nil,
  toggle = fn -> this.completed = not(this.completed)
}

{- This is an example of a multi-line comment. It'll be ignored by the compiler.
__new__ is special field which when available is used for creating record instances
__doc__ is for attaching docs to objects
-}
-- todo states
let todo = {
  state = :active,
  items = []
--  __new__ = foo with state { state = :all, items= [] }
--   __doc__ = "Object to represent a task"
}

let main =
  print todo.state
